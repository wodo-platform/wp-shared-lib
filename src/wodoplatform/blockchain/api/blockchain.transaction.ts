/**
 * Main interface to define blockchain transactions in Wodo Platform. Different blockchain networks implement
 * tansactions in different ways. Hence this primary interface helps managing transactions flowing through the wodo platform
 */
export interface BlockchainTransaction {
} 